#include <stdio.h>
int main() {
    int a[5]={1,9,3,8,5}, max=0, second=0;
    for(int i=0;i<5;i++){
        if(a[i]>max){
            second=max;
            max=a[i];
        }
        else if(a[i]>second && a[i]!=max)
            second=a[i];
    }
    printf("Second Largest = %d", second);
}
