#include<stdio.h>
int main()
{
    int a[5],i,*p,sum=0;
    float avg;

    p=a;

    for(i=0;i<5;i++)
        scanf("%d",p+i);

    for(i=0;i<5;i++)
        sum=sum+*(p+i);

    avg=sum/5.0;

    printf("Average=%.2f",avg);

    return 0;
}
