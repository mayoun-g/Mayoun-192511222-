#include<stdio.h>

int largest(int *a,int *b)
{
    if(*a>*b)
        return *a;
    else
        return *b;
}

int main()
{
    int x=10,y=20;

    printf("Largest=%d",largest(&x,&y));

    return 0;
}
