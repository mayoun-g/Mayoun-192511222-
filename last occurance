#include<stdio.h>
void main()
{
    char str[100],ch;
    int i,pos=-1;

    printf("Enter string: ");
    scanf("%s",str);

    printf("Enter char: ");
    scanf(" %c",&ch);

    for(i=0;str[i]!='\0';i++){
        if(str[i]==ch)
            pos=i;
    }

    printf("Last position=%d",pos);
}
