#include<stdio.h>
int main()
{
    int a[5],i,sum=0,*p;

    p=a;

    for(i=0;i<5;i++)
        scanf("%d",p+i);

    for(i=0;i<5;i++)
        sum=sum+*(p+i);

    printf("Sum=%d",sum);

    return 0;
}
